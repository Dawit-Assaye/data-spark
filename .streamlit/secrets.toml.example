# Streamlit Cloud Secrets Template
# Copy this file and rename to secrets.toml (don't commit the actual secrets.toml!)
# Or add secrets directly in Streamlit Cloud UI: App Settings → Secrets

GEMINI_API_KEY = "your_gemini_api_key_here"

